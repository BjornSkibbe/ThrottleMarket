export { default } from '@/features/messaging/pages/page'

// when you need a page to be rendered fresh on every request 
// and you don't want Next.js to cache or statically optimize it.
export const dynamic = 'force-dynamic' 

